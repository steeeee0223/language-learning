# Fumadocs Initial Scaffold Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Initialize a Next.js + Fumadocs project in the current workspace and commit the scaffold.

**Architecture:** Use the official `create-fumadocs-app` CLI to create a Next.js/Fumadocs MDX app directly in the workspace. Keep the previously approved design spec and this plan in `docs/superpowers`, then make an initial Git commit containing the scaffold and planning documents.

**Tech Stack:** Node 24, pnpm 11, TypeScript 6, React 19, Next.js, Fumadocs, MDX, Git.

---

### Task 1: Initialize Fumadocs Scaffold

**Files:**
- Create: scaffold files generated by `create-fumadocs-app`
- Preserve: `docs/superpowers/specs/2026-06-26-language-learning-video-app-design.md`
- Preserve: `docs/superpowers/plans/2026-06-27-fumadocs-initial-scaffold.md`

- [ ] **Step 1: Confirm CLI options**

Run:

```bash
npm create fumadocs-app@latest -- --help
```

Expected: output includes the `+next+fuma-docs-mdx` template and package manager options.

- [ ] **Step 2: Initialize the app**

Run:

```bash
corepack pnpm create fumadocs-app . --src --template +next+fuma-docs-mdx --pm pnpm --install --no-git --linter eslint --search orama --og-image next-og
```

Expected: scaffold files are created and pnpm dependencies are installed.

- [ ] **Step 3: Initialize Git**

Run:

```bash
git init
```

Expected: `.git` exists in the workspace.

- [ ] **Step 4: Inspect generated files**

Run:

```bash
git status --short
```

Expected: scaffold files and docs are untracked.

- [ ] **Step 5: Commit scaffold**

Run:

```bash
git add .
git commit -m "chore: initial fumadocs scaffold"
```

Expected: initial commit succeeds.

## Self-Review

- Spec coverage: this plan only covers the user's immediate scaffold and initial commit request. The full MVP implementation remains covered by the approved design spec and should get a separate implementation plan.
- Placeholder scan: no placeholder steps remain.
- Type consistency: no application types are introduced in this scaffold-only plan.
